meta {
  name: Listing with fields
  type: http
  seq: 2
}

get {
  url: http://eits.local:8001/api/resource/Lead?fields=["first_name", "lead_owner"]&debug=True
  body: none
  auth: inherit
}

params:query {
  fields: ["first_name", "lead_owner"]
  debug: True
}
